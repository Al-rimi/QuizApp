<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">

        <!-- SignInActivity: The main entry point of the application -->
        <activity
            android:name=".SignInActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ExamSelectionActivity: Allows users to choose a quiz category -->
        <activity android:name=".ExamSelectionActivity" />

        <!-- QuizActivity: Where the quiz questions are displayed and answered -->
        <activity android:name=".QuizActivity" />

        <!-- QuizResultActivity: Shows the final quiz score -->
        <activity android:name=".QuizResultActivity" android:exported="false" />


    </application>
</manifest>
